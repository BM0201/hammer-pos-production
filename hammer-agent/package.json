{
  "name": "hammer-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "H.A.M.M.E.R. — Agente de sucursal para el módulo Cámaras (descubrimiento ONVIF, salud de stream, relay). Se escribe en TypeScript portable (Node-compatible) y se compila a binario único con `bun build --compile` en una máquina con Bun instalado.",
  "scripts": {
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "build:bun": "bun build ./src/main.ts --compile --outfile hammer-agent"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "tsx": "^4.22.4",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22 <23"
  }
}

/**
 * Contrato versionado del heartbeat (adenda §2.3) — espejo del mismo
 * archivo en hammer-agent/src/heartbeat/protocol.ts. "El servidor tolera
 * versiones viejas... esto tiene que existir desde el primer commit."
 */
export const AGENT_PROTOCOL_VERSION = 1;
